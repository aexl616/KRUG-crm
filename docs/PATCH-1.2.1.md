# Patch 1.2.1 — Settings Navigation & Cleanup

## Исправления

- app.js: операционный пункт «Выплаты» восстановлен в базовой навигации для всех ранее допущенных ролей через существующий canViewSection. В Settings финансовые правила видит только owner; пустая вкладка администратора исключена. Названия правил определены непосредственно в Settings, без переименования операционного раздела.
- ux.js: видимая навигация называет раздел «Выплаты»; удалено дублирующее переименование финансовых вкладок через замену HTML-строки.
- normalizeLegacyState: удалён повторный `...previous` клиента, один spread сохранён.
- tests/settings.test.cjs: три регрессионные проверки базовой навигации, owner-only вкладок и видимости только собственных выплат engineer.

Новых функций нет. Расчёты выплат, Finance, hourly services, data layer и матрица операционных прав не изменены.

## Проверки

169 автоматических тестов прошли: 24 data layer, 42 finance, 29 settings, 46 hourly, 28 notifications. git diff --check без ошибок.

Изолированный Edge, тестовые данные: owner открывает отдельные выплаты; все три вкладки Settings открываются и сохраняют реальные изменения (минимум выплаты, имя кошелька, имя студии), значения сохраняются после reload. Admin открывает операционные выплаты. Engineer видит личный экран и пункт «Выплаты» в мобильном меню, но не Settings. Мобильный экран 390×844 без горизонтального переполнения. Ошибок JavaScript в проверенных сценариях нет.

Полный ручной цикл проведения записей в этом навигационном патче не повторялся; существующие автоматические проверки прошли. Ограничения localStorage и клиентских ролей прежние. Публикация на GitHub не выполнялась.
